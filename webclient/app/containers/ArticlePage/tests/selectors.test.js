// import { fromJS } from 'immutable';
// import { makeSelectArticlePageDomain } from '../selectors';

// const selector = makeSelectArticlePageDomain();

describe('makeSelectArticlePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
